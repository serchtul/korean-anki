[project]
name = "korean-anki"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "genanki>=0.13.1",
]

[project.scripts]
anki-kr = "korean_anki.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
